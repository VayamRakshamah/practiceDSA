DSA practice Readme file 


What is the difference between:

fixed-size sliding window

variable-size sliding window

When do we use sliding window instead of two pointers?

In sliding window problems, what are the 3 main things you always track? 